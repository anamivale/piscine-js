// const _ = require("lodash");
// const debounce = (str) =>{

// }
// const processInput = _.debounce(function (text) {
//   console.log("User input:", text);
// }, 3000, { leading: false, trailing: true});
// processInput("name")
// // Example: Attach debounce to an input field
// inputElement.addEventListener("input", (e) => {
//   processInput(e.target.value);
// });

const debounce = setTimeout(() => {}, 3000);

const opDebounce = setTimeout(() => {}, 0);
